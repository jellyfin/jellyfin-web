export const defaultExports = {
    centerFocus: {
        on: (_elem: HTMLElement, _horiz: boolean) => {},
        off: (_elem: HTMLElement, _horiz: boolean) => {}
    }
};

export const centerFocus = {
    on: (_elem: HTMLElement, _horiz: boolean) => {},
    off: (_elem: HTMLElement, _horiz: boolean) => {}
};

const scrollHelper = defaultExports;
export default scrollHelper;
